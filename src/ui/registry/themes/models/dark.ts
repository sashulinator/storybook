import { DARK } from '~/shared/theme'

import { Theme } from '..'

// prettier-ignore
export const dark = {
  'ui-Registry-item_bg':                                  DARK.bgSecondary.darken(0.15),
  'ui-Registry-item_bg--heighlighted':                    DARK.bgSecondary,
} satisfies Theme
