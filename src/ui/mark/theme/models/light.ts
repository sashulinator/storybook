import { lightTheme } from '~/ui/field'

import { Theme } from '..'

// prettier-ignore
export const light = {
  'ui-Mark_highlight':                                   lightTheme.field_bg,
  'ui-Mark_highlight--hover':                            lightTheme.field_bg__hover,
} satisfies Theme
