import { darkTheme } from '~/ui/field'

import { Theme } from '..'

// prettier-ignore
export const dark = {
  'ui-Mark_highlight':                                   darkTheme.field_bg,
  'ui-Mark_highlight--hover':                            darkTheme.field_bg__hover,
} satisfies Theme
