import dark from '~/shared/themes/colors/colibri/dark'

import type { Colors } from '../../types/colors'

export const colors = {
  'Hint_color--error': dark.errorColor,
  'Hint-color--success': dark.successColor,
} satisfies Colors

export default colors
